project: rnaseq-framework

# Inputs
fastq_dir: "data/raw"
samples_tsv: "data/metadata/samples.tsv"

# Reference (transcriptome FASTA)
ref_dir: "resources/ref"
# Use "current" GENCODE human release page as your source-of-truth;
# we store the direct URL here so the workflow is fully reproducible.
# GENCODE gene annotation (for tx2gene)
transcriptome_fasta_gz_url: "https://ftp.ebi.ac.uk/pub/databases/gencode/Gencode_human/release_49/gencode.v49.transcripts.fa.gz"
annotation_gtf_gz_url: "https://ftp.ebi.ac.uk/pub/databases/gencode/Gencode_human/release_49/gencode.v49.annotation.gtf.gz"

# Salmon outputs
salmon_index_dir: "resources/salmon_index"
salmon_quant_dir: "results/quant"

threads: 4
